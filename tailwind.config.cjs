/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ['./src/**/*.{html,js,svelte,ts}'],
  // DaisyUI plugin is now loaded via @plugin in app.css (Tailwind v4 CSS-first config)
};